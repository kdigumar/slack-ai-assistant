apiVersion: v1
kind: Service
metadata:
  name: slack-assistant
  namespace: slack-assistant
  labels:
    app.kubernetes.io/name: slack-assistant
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: slack-assistant

